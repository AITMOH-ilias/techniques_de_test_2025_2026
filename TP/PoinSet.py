# Les 4 premiers bytes représentent un unsigned long donnant le nombre de points dans l'ensemble
# Les bytes suivants représentent les points, avec pour chaque point 8 bytes. Les 4 premiers bytes sont la coordonnée X (un float) et les 4 bytes suivant la coordonnée Y (un float aussi).
# Triangles
class PointSet:
    def __init__(self,points):
        # self.points = points or []
        # a implementer 
        pass
        
    def get_points(self):
        # return self.points
        # a implementer
        pass
    
    
    
    
    